Cú pháp chạy train mô hình
python methods/method_ladeda.py `
>>   --data-root "D:\CelebDF_Frames_Split_Correct" `
>>   --out-dir outputs_ladeda `
>>   --epochs 15 `
>>   --batch-size 32 `
>>   --lr 1e-4

Cú pháp chạy backend gọi API (Chưa sửa API)
cd backend
python app.py

Cú pháp chạy frontend (ReactJS)
cd frontend
npm start
